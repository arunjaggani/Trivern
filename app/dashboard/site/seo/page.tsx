"use client";
import ComingSoonPage from "@/components/ComingSoonPage";
import { FileSearch } from "lucide-react";
export default function SEOSettingsPage() {
    return <ComingSoonPage title="SEO Settings" description="Configure meta defaults, sitemap settings, and search engine indexing preferences." icon={FileSearch} />;
}
