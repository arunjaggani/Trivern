import ComingSoonPage from "@/components/ComingSoonPage";
import { Zap } from "lucide-react";
export default function BookingEnginePage() {
    return <ComingSoonPage title="Booking Engine" description="Configure meeting rules, slot logic, buffer rules, and availability settings for your booking system." icon={Zap} />;
}
